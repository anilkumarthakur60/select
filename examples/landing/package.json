{
  "name": "example-landing",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Framework-neutral landing page for @anil-labs/select — every demo on it runs the web component and the core tree helpers, so it needs no framework runtime.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-page.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anil-labs/select-core": "workspace:*",
    "@anil-labs/select-element": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "jsdom": "^30.0.1",
    "typescript": "~6.0.0",
    "vite": "^8.2.1"
  }
}
